#include <stdio.h>
int main()
{
  int x=50;
  printf("value of x is:%d",x);
  x=100;
  printf("value of x is:  %d",x);
  x=200;
  printf("value of x is:  %d",x);
   x=300;
  printf("value of x is:  %d",x);
  return 0;
}