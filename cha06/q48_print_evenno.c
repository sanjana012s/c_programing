#include <stdio.h>

int main()
{
    int i, n;
    int sum = 0;
    int n1;
    int store;

    // Input for how many numbers to enter
    printf("enter the no:");
    scanf("%d", &n);

    for(i = 1; i <= n; i++)
    {
      if(i%2==0)
      {
         printf("%d\t",i);
      }
     
      continue;
  }

}
   
